language: node_js
node_js:
  - "6"
  - "8"
  - "10"
sudo: false
# disable coverage as it is broken
# script: "gulp test && gulp coverage"
script: "gulp test"
after_success:
  - npm install -g codeclimate-test-reporter
  - codeclimate-test-reporter < coverage/lcov.info
